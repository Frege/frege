module Compiler.natives.types.PlaceholderSymbol where

import Compiler.types.QNames(QName())
import Compiler.types.Positions(Position())

--- Represents a temporary symbol added to the symbol table which we try to
--- generate a native declaration for.
data PlaceholderSymbol = PlaceholderSymbol { nativeName :: String, parentName :: QName, pos :: Position }
